# Порівняння ефективності алгоритмів пошуку підрядка

**Данні**
стаття 1: 12644 символів ./text/стаття 1.txt
стаття 2: 17615 символів ./text/стаття 2.txt

**Результати**
| Місце | Алгоритм | Стаття 1 (існ.) | Стаття 1 (виг.) | Стаття 2 (існ.) | Стаття 2 (виг.) | Середній час |
|-------|----------|-----------------|-----------------|-----------------|-----------------|--------------|
| 🥇 | Boyer-Moore | 0.001076 с | 0.016997 с | 0.013958 с | 0.025549 с | **0.014 с** |
| 🥈 | Knuth-Morris-Pratt | 0.002943 с | 0.151346 с | 0.037823 с | 0.226840 с | 0.105 с |
| 🥉 | Aho-Corasick | 0.147745 с | 0.137522 с | 0.203285 с | 0.200749 с | 0.172 с |
| 4 | Rabin-Karp | 0.005600 с | 0.331022 с | 0.084921 с | 0.473606 с | 0.224 с |
| 5 | Hybrid Optimized | 0.006570 с | 0.464201 с | 0.086055 с | 0.686165 с | 0.311 с |
| 6 | Hybrid (RK+BM) | 0.006905 с | 0.488509 с | 0.090946 с | 0.716181 с | 0.326 с |

**Boyer-Moore** домінує через евристику "поганого символу" — коли символ не співпадає, алгоритм може одразу зсунутись на кілька позицій вперед, замість перевірки кожного символу, що особливо ефективно на кириличному тексті з великим алфавітом.

**Knuth-Morris-Pratt** програє через необхідність перевіряти кожен символ послідовно, хоча його LPS-таблиця запобігає повторним перевіркам при невдачах.

**Aho-Corasick** витрачає час на побудову складного автомата (trie + failure links), що виправдано лише при пошуку десятків патернів одночасно, а не одного.

**Rabin-Karp** сповільнюють постійні обчислення хешів для кожного вікна тексту, навіть якщо швидке порівняння через хеш теоретично має бути ефективним.

**Гібридні алгоритми** (Rabin-Karp + Boyer-Moore) виявились найповільнішими, оскільки намагаються поєднати два підходи, але насправді виконують подвійну роботу: спочатку обчислюють хеш для порівняння (як RK), потім все одно перевіряють символи від кінця (як BM), плюс будують таблицю зсувів. Замість синергії отримали накладні витрати обох методів, що особливо помітно на вигаданих підрядках (0.464-0.716 с), де алгоритм змушений обробляти весь текст з усіма "прикрасами".

# Homework: Greedy vs Dynamic Programming

[Завдання](https://www.edu.goit.global/learn/38442047/19646173/19658335/homework)

---

## 📊 Висновок

Жадібний алгоритм для підготовлених даних є **швидшим** і займає **менше пам'яті**.
Однак з непідготовленими даними він може повернути **неоптимальне рішення**.

Як і з усіма алгоритмами, головне — знати, **в яких випадках** його краще використовувати.
Якщо жадібний алгоритм не працює, застосовуйте **Динамічне програмування** — воно гарантує точність і надійність, хоча потребує більше часу та пам'яті.

### **Greedy, якщо:**
- ✅ **Довів**, що алгоритм працює для твоєї задачі
- ✅ Швидкість критична
- ✅ Вхідні дані спеціальні (canonical coins, MST, Dijkstra)

### **DP, якщо:**
- ✅ Потрібен **гарантований оптимум**
- ✅ Жадібний алгоритм **не працює** для твоїх даних
- ✅ Є повторювані підзадачі

---

## 🟢 Жадібний алгоритм (Greedy)

### **Принцип:**
- На кожному кроці вибираємо **локально оптимальне** рішення
- **Не повертаємось** назад (no backtracking)
- Сподіваємось, що локальні оптимуми → глобальний оптимум

### **Приклад задачі:**
Треба сортувати предмети за їхнім співвідношенням **вартість/вага** та обирати найкращі, поки не заповниться рюкзак.
Непоганий метод, але **не завжди** знаходить оптимальне рішення.

### **Коли використовувати:**
✅ **Greedy choice property** — локальний оптимум веде до глобального
✅ **Canonical coin systems** (USD, EUR, UAH)

### **Переваги:**
- ⚡ Дуже швидкий — **O(n log n)**
- 💾 Мінімум пам'яті — **O(1)**
- 🧩 Простий код

### **Недоліки:**
- ❌ **НЕ гарантує** оптимум для всіх даних
- ❌ Потрібно **довести** коректність для конкретної задачі

### **Приклади задач:**
- Activity selection
- Huffman coding
- Dijkstra's algorithm (найкоротший шлях з додатними вагами)
- Coin change (тільки canonical systems)

---

## 🔵 Динамічне програмування (DP)

### **Принцип:**
- Розбиваємо задачу на **підзадачі**
- **Зберігаємо** результати (уникаємо повторних обчислень)
- Будуємо рішення **знизу вгору** або **зверху вниз** (мемоїзація)

### **Приклад задачі:**
Створюємо таблицю, яка відображає максимальну вартість для кожної підмножини місткості рюкзака, використовуючи перші N предметів.
Обчислюємо значення рекурсивно та використовуємо попередні обчислення для знаходження **оптимального набору** предметів.

### **Коли використовувати:**
✅ **Overlapping subproblems** — підзадачі повторюються
✅ **Optimal substructure** — оптимум складається з оптимумів підзадач

### **Переваги:**
- ✅ **Гарантує оптимальне** рішення
- ✅ Працює для **будь-яких вхідних даних**

### **Недоліки:**
- ⏱️ Повільніший — **O(n × m)**
- 💾 Більше пам'яті — **O(n)**

### **Приклади задач:**
- Coin change (будь-які монети)
- Knapsack problem
- Fibonacci numbers
- Shortest path (з від'ємними вагами)
